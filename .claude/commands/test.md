Run the project's test suite and report results.

Steps:
1. Switch to QA mode (or stay if already in QA)
2. Identify the test command from the project (check package.json, README, or ask user)
3. Request user to run tests with clear command
4. Wait for test results
5. Analyze results
6. Document in STATUS.md if tests fail
7. Report summary:
   - ✅ Tests passing: X/Y
   - ❌ Tests failing: List failures
   - ⚠️ Warnings or flaky tests
   - 📊 Coverage (if available)
8. Recommend next steps based on results
